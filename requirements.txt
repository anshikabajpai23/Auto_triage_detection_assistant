# ---------------------------------------------------------------
# Core ML stack — install this on BigRed200 (A100, Linux/CUDA)
# ---------------------------------------------------------------

# HuggingFace ecosystem
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.29.0
tokenizers>=0.19.0
huggingface-hub>=0.22.0

# Fine-tuning / PEFT / QLoRA
peft>=0.10.0
bitsandbytes>=0.43.0          # 4-bit NF4 quantization — Linux/CUDA only

# RLHF / PPO / Reward model
trl>=0.8.0

# Data wrangling
pandas>=2.2.0
scikit-learn>=1.4.0
numpy>=1.26.0

# Evaluation / visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# Experiment tracking
wandb>=0.16.0

# UI
streamlit>=1.32.0

# Utilities
scipy>=1.12.0
tqdm>=4.66.0
pyyaml>=6.0.1

# PyTorch — install separately via the official command for your CUDA version:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
