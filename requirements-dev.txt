# ---------------------------------------------------------------
# Local development — macOS (Apple Silicon / Intel)
# No bitsandbytes (CUDA-only), no GPU training
# Use this to develop and test the data pipeline and UI locally
# ---------------------------------------------------------------

# HuggingFace ecosystem
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.29.0
tokenizers>=0.19.0
huggingface-hub>=0.22.0

# Fine-tuning / PEFT (no bitsandbytes — not supported on macOS ARM)
peft>=0.10.0

# RLHF (install without CUDA extensions)
trl>=0.8.0

# Data wrangling
pandas>=2.2.0
scikit-learn>=1.4.0
numpy>=1.26.0

# Evaluation / visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# Experiment tracking
wandb>=0.16.0

# UI
streamlit>=1.32.0

# Utilities
scipy>=1.12.0
tqdm>=4.66.0
pyyaml>=6.0.1

# PyTorch CPU (for local data pipeline testing)
# pip install torch torchvision torchaudio

# Notebook support
jupyter>=1.0.0
ipykernel>=6.29.0
